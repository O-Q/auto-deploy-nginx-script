# Auto Static Files Deployment

It will install and configure all dependencies(nginx, node, etc.).
For example, built angular, react, vue, etc. project.

## Directory tree for static project

```bash
├── run-static.sh
│
├── static-nginx.conf
|
└── dist
      |
      └── <PROJECT_NAME>
              |
              └── PROJECT FILES...
```
